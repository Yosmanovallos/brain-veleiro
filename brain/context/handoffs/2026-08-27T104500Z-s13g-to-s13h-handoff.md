# BRAIN — HANDOFF

> Structured transfer artifact for continuing work safely without replaying the full conversation.

## Metadata

**Handoff ID:**
`HANDOFF-S13G-TO-S13H`

**Created at:**
`2026-08-27T10:45:00Z`

**Created by:**
`claude-code (primary_builder)`

**Status:**
`VERIFIED`

# 1. objective

Close S13G (task-prompt-compiler) with `PASS` after integrating Part A verbatim (already committed at
`f7ef335`) and implementing Part B (the `src/intelligence/task-prompt-compiler/` module, a new typed
`intelligence.task-prompt-compiler.s13g` Skill, `SKILL-ARTIFACT-1..4` + T1–T50 + 4 anchors, verification
report, an independent review pass), commit and push. **Do not start S13H.** S13G is not considered
independently verified until a fresh read-only session returns
`VERIFICATION RESULT / Step: S13G / Status: PASS`.

# 2. branch/HEAD/status

**Repository root:**
`/mnt/c/Users/yosma.DESKTOP-RQ0SDF3/Documents/brain-veleiro` (GitHub: `Yosmanovallos/brain-veleiro`)

**Branch:**
`main`

**Part A commit:**
`f7ef33537ef5d61402f6d6b8a783db6da25db7b2` — `docs: integrate Brain S13G Part A (ChatGPT authoring)`

**HEAD (before this closure's commit):**
`f7ef33537ef5d61402f6d6b8a783db6da25db7b2`

**Worktree status:**
`Verified CLEAN after this closure's commits — see the COMMIT section of the final STEP_STATUS response for the exact resulting sha.`

**Verification status:**
`VERIFIED (primary-builder deterministic QA). Fresh independent verification: PENDING.`

# 3. verified completed work

| ID | Completed item | Verification | Evidence ref |
|---|---|---|---|
| CW-001 | S00–S13F all `PASS` | `brain-bootstrap/STATE.yaml` `steps` block | `brain-bootstrap/STATE.yaml` |
| CW-002 | S13G Part A integrated verbatim at the 3 canonical paths | `git diff f7ef335 -- <the 3 files>` **empty** at closure; `SKILL-ARTIFACT-1..4` assert existence, non-triviality, canonical headers, YAML/spec structure | `brain-bootstrap/skills/TASK_PROMPT_COMPILER_SKILL_S13G.md`, `brain-bootstrap/quality-contracts/S13G_TASK_PROMPT_COMPILER_DEEP.yaml`, `brain-bootstrap/specs/EXECUTION_PACKAGE_CONTRACT_S13G.md` |
| CW-003 | S13G Part B implementation (15 new `src/intelligence/task-prompt-compiler/` files + 1 new Skill def + 1 modified `skills/index.ts`) — `SKILL_ONLY`, **no** new `AgentDefinition`, no `src/core/` change, no new dependency | typecheck 0 errors, 533/533 tests pre- and post-build | `brain-bootstrap/reports/S13G-task-prompt-compiler-verification.md` §3, §9 |
| CW-004 | `SKILL-ARTIFACT-1..4` + T1–T50 + 4 helper anchors (81 `it()` cases) all PASS | `npx vitest run tests/task-prompt-compiler/` → 81/81 | same report §8 (full T1→T50 mapping) |
| CW-005 | Independent (advisor) review run before PASS; 1 **blocking** Part B defect found and fixed (`validateExecutionPackage` only spot-checked `instructions` per-instruction — a lazy compile could drop every SPEC/CONSTRAINT/SKILL instruction and still validate; spec §7.2 makes completeness normative). `HI-014` extended to completeness (D-060); regression added to `T23`. Negative control run at closure: the test fails at `expect(v.valid).toBe(false)` with the completeness block disabled, passes with it restored, `git status` clean again. Plus 3 decorative frozen-truth fields wired into live assertions + minor cleanups. Re-run 533/533 after the fix. | same report §7 |
| CW-006 | Skill-vs-no-Skill improvement proven against frozen independent ground truth: baseline **56/108** → skill **108/108** (`+52`), **9/9** improved dimensions (min `+2`), hard invariants **48/48 (100%)**, **0** stage-boundary violations, **0** invented authority/tool/limit/schema refs, `meets_threshold: true`, `hard_invariant_regressed: false` | `T48`/`T49` assert exact figures; measured through the Vitest suite against post-review source, corroborated by clean build + post-build full-suite re-run (identical results) | same report §6 |
| CW-007 | S13G closed `PASS` in `brain-bootstrap/STATE.yaml` | `repository.task_prompt_compiler.status: PASS`, `steps.S13G: PASS`, `steps.S13H: NOT_STARTED` (unchanged), `current_step: S13G`. Stale `repository:` block refreshed mechanically in the `docs:` follow-up commit | `brain-bootstrap/STATE.yaml` |
| CW-008 | Transfer file `S13G_CHATGPT_PART_A_CANONICAL.md` sha256 `fc5ef8e83e6669d06727785a4c17442543a1403ca3c1a4d81ce1bbd7b3a3417f` unchanged; **not** included in any implementation commit; retained untracked until the final fresh independent verification | `sha256sum` at closure | this session |

# 4. commands/evidence

| ID | Command / Method | Result |
|---|---|---|
| EV-001 | `git rev-parse HEAD` / `git status --porcelain` / `git diff f7ef335 -- <3 Part A files>` | HEAD `f7ef335` at session start; Part A diff empty |
| EV-002 | `node --version` (after prepending `/home/yosman/.nvm/versions/node/v24.19.0/bin` to PATH) | `v24.19.0` |
| EV-003 | `npm run typecheck` (pre-impl, post-impl, pre-build, post-build, pre- and post-review-fix) | `tsc --noEmit`, 0 errors every time |
| EV-004 | `npm test` pre-build | `533/533` (452 pre-existing + 81 new) |
| EV-005 | `rm -rf dist && npm run build` | succeeded |
| EV-006 | `npm test` post-build | `533/533`, unchanged |
| EV-007 | `npx vitest run tests/task-prompt-compiler/` | `81/81` |
| EV-008 | grep `src/core/` for `task-prompt-compiler` / `TaskCompilation` / `S13G` / compiler role branch; forbidden cross-layer import grep | all empty |
| EV-009 | grep `src/intelligence/task-prompt-compiler/` for `agent-definitions` | only a comment stating it is **not** imported |
| EV-010 | grep for `s13h` / `repository-git-workflow` / `agent factory` / `skill factory` / `task executor` / `workflow runtime` / `execution graph` in the S13G module + Skill def | only in negative contexts (`TC-R20`, `TC-V9`, `exclusions`) |
| EV-011 | `git diff --stat package.json package-lock.json` | empty (no new dependency) |
| EV-012 | `sha256sum tests/task-prompt-compiler/fixtureTruth.ts` — file unmodified across this session, so the hash pins the exact content that produced the §6 figures; `T46` proves it never reaches the runtime path | `11761ba2a90bdd86992f008cde0ef1c16bcfa3ba13bb18c884cfa97119b7915a` |
| EV-013 | `advisor()` call before declaring PASS; completeness hole proven: strip instructions to TASK+SAFETY → `valid:false` with `HI-014` error; restored → correctly accepted | 1 blocking Part B defect found and fixed |

# 5. decisions

All decisions from prior handoffs remain in force and were not reopened. Five new Part B-scoped
determinizations (recorded in the verification report §3.2):

| Decision ID | Decision | Status | Rationale |
|---|---|---|---|
| D-056 | The single `assembleExecutionPackage` synthesizer's behaviour is parameterized by `deriveAssemblyProfileFromRules()`, a CONTENT-regex read of whatever Skill rule text the materialized run objective carries. No-Skill objective → all profile fields `false` (naive); with-Skill objective (embeds the serialized Skill body) → all `true` (faithful). | VERIFIED | Satisfies "no separate intentionally-bad baseline compiler", "no `withSkill` boolean branch", "no Skill-ID/fixture-ID branch inside the provider". Proven by the `helpers — assembly profile derivation is content-driven` anchor + `T47`. |
| D-057 | `gateTaskCompilation()` never reads `candidate.status`; readiness is the deterministic result of compatibility + context-pack validation + full `validateExecutionPackage`. | VERIFIED | Anti-self-certification (S13B/D/F). Proven by the `helpers — the gate never trusts the model status` anchor. |
| D-058 | S13G's package forbidden-key list `EXECUTION_PACKAGE_FORBIDDEN_KEYS` is a **new** Stage-12+/S14/S17-facing list (`provider`, `connector`, `mcp_server`, `credential`, `token`, `runtime_handle`, `execution_result`, `workflow_state`, `task_executor`, `acceptance_passed`, `tests_passed`, …), distinct from S13F's `STAGE_11_FORBIDDEN_KEYS` (which forbids `tools`/`context`/`instructions`/`limits`/`selected_skills` — exactly what S13G legitimately outputs). | VERIFIED | The S13F name-collision would otherwise reject every valid S13G package. Documented in `S13G_AUTHORING_PREFLIGHT.md`. |
| D-059 | Frozen `tests/task-prompt-compiler/fixtureTruth.ts` carries deliberate slack: `min_skill_must_instructions: 2` while the faithful compile emits 3 and the naive arm emits 0. | VERIFIED | Threshold-bearing values must not be tuned to a single exact figure (S13F review lesson). |
| D-060 | `HI-014` enforces instruction **completeness** (spec §7.2 "Instructions MUST preserve, when applicable, …"), not only per-instruction provenance: every task-material Spec/NFR ref, every cited constraint ref, and every selected-Skill MUST rule id must be represented by some instruction, and a TASK instruction must cite `task:<id>`. | VERIFIED | Independent review (R-1, blocking): the per-instruction spot check alone left `instructions` — the one field with model latitude — able to silently drop SPEC/SKILL entries. Regression `T23` case 2. |

# 6. open issues

None blocking. Disclosed, non-blocking limitations (full detail in the verification report §11):

| Issue ID | Issue | Impact | What would resolve it |
|---|---|---|---|
| OI-S13G-01 | `T12` is exercised by a direct `validateTargetExecutionCompatibility` call, not the end-to-end path (structurally unreachable — `src/core/agent/validateDefinition.ts` enforces `set(tools) == set(capabilities)`) | NONE (clause still proven; disclosed in report §8) | A Core change allowing `tools ≠ capabilities`, which is not desired |
| OI-S13G-02 | The verification "model" is a deterministic rule-based `ModelProvider` fixture, not a real LLM | LOW (permitted by QC `reference_model_policy`; runs genuinely inside `runAgent()`) | A real `ModelProvider`-driven reasoner behind the same contract |
| OI-S13G-03 | The Skill-vs-no-Skill comparison is a content-derived maximal contrast (one synthesizer, profile derived by regex from rule text), same accepted pattern as S13A–S13F | LOW (frozen fixture-truth asserted `T46` to never reach the runtime path; not emergent measurement) | A second independent general compiler, if a future step needs one |
| OI-S13G-04 | `deriveAssemblyProfileFromRules` uses phrase/word-boundary regex, not semantic understanding | LOW (same class as S13C's disclosed cue checks; both extreme arms proven, the middle not claimed) | A real reasoner or a stricter parser |
| OI-S13G-05 | `fixtures.ts` and `fixtureTruth.ts` land in the same implementation commit (as S13F), so `HAND_AUTHORED_BEFORE_RUN` is not provable from git history alone | LOW | sha256 recorded in report §5 before §6 figures; `T46` proves non-import; `T47` proves no separate baseline compiler |
| OI-S13G-06 | `STATE.yaml` `repository:` block was stale (`commits: 10`, `head_sha: fbd5af95…`) since before S13F | NONE | Refreshed mechanically in the S13G closure `docs:` follow-up commit |

# 7. changed files

| Path | Change | Notes |
|---|---|---|
| `src/intelligence/task-prompt-compiler/constants.ts` | ADDED | Skill id / QC ref / artifact paths / parse markers / `EXECUTION_PACKAGE_SCHEMA_VERSION` / `EXECUTION_PACKAGE_FORBIDDEN_KEYS` (new Stage-12+/S14/S17-facing list) |
| `src/intelligence/task-prompt-compiler/types.ts` | ADDED | `TaskCompilationInput` + `ExecutionPackage` (8 required categories) + `ExecutionInstruction` + comparison types + test-only `TaskCompilationFixtureTruth` + `NON_NORMATIVE_CONTEXT_LAYERS` |
| `src/intelligence/task-prompt-compiler/sharedNormalization.ts` | ADDED | `stableStringify`, `deepClone`, acceptance/evidence normalize+equal, `jsonSchemaEqual`, `isMaterialRef`, `taskMaterialSpecRefs`, `boundedSpecSnapshotRefs`, `computePackageId`, `findExecutionPackageForbiddenKeys` (deep KEY scan), `containsKnownSecretValue` (explicit tagged/known patterns only) |
| `src/intelligence/task-prompt-compiler/projectTaskCompilationSpec.ts` | ADDED | full S13F spec snapshot → only task-material refs (+ transitive acceptance); no new fact, no widening |
| `src/intelligence/task-prompt-compiler/validateTaskCompilationInput.ts` | ADDED | STRUCTURAL defects throw; approval/mismatch/unknown-ref are BLOCKED results; calls `validateAgentDefinition` + `validateSkillDefinition` |
| `src/intelligence/task-prompt-compiler/validateContextPackSnapshot.ts` | ADDED | 9 authority ranks in order, ≥1 concrete positive budget bound, item id/layer/rank/status/provenance/relevance, essential-item BLOCKED/UNKNOWN → blocker, objective word-overlap with `task.outcome` |
| `src/intelligence/task-prompt-compiler/validateTargetExecutionCompatibility.ts` | ADDED | spec §5.1–5.10 + §14 blockers (readiness, approval, bounded projection, acceptance/evidence deep-equal, constraint resolution, agent-ref match, selected-Skill allowlist/uniqueness, capability ∈ agent.capabilities ∧ agent.tools, Skill requires ⊆ supplied, Skill allowed_capabilities ⊇ requires, known-secret scan) |
| `src/intelligence/task-prompt-compiler/materializeExecutionTools.ts` | ADDED | `[{id, capability_ref}]` per unique capability id, sorted — provider-neutral, unbound |
| `src/intelligence/task-prompt-compiler/materializeExecutionLimits.ts` | ADDED | `{max_turns, timeout_ms}` from `agent.limits`; `context_budget` = clone of pack budget |
| `src/intelligence/task-prompt-compiler/compileExecutionInstructions.ts` | ADDED | `InstructionAssemblyProfile`; faithful/naive profiles; `isEligiblePolicyContextItem`; `assembleExecutionInstructions` emits TASK/SPEC/CONSTRAINT/SKILL/POLICY/SAFETY with source refs |
| `src/intelligence/task-prompt-compiler/assembleExecutionPackage.ts` | ADDED | the **single** synthesizer; `ExecutionAssemblyProfile` (9 booleans); `deriveAssemblyProfileFromRules` (CONTENT regex over rule text — no with-Skill/Skill-id/fixture-id branch); naive path broadens objective/widens refs/trims LOW context/binds provider/adds schema prop/+2 max_turns/rewrites acceptance |
| `src/intelligence/task-prompt-compiler/validateExecutionPackage.ts` | ADDED | `HI-001..HI-026` deterministic validator; recomputes every package field from the bounded input; forbidden-key deep scan; instruction provenance **and completeness** (HI-014, D-060); `mapTaskCompilationResultToStructuredOutput` |
| `src/intelligence/task-prompt-compiler/compileTaskExecutionPackage.ts` | ADDED | `materializeTaskCompilationTask` / `materializeBaselineTaskCompilationTask`; `gateTaskCompilation` (never trusts `candidate.status`); `compileTaskExecutionPackage(input, harness)` (S12→S10→S09). `../agent-definitions/` not imported |
| `src/intelligence/task-prompt-compiler/compareTaskCompilationRuns.ts` | ADDED | 27 discrete assertions `A01..A27` across 9 `SD-00x` dimensions vs frozen fixture truth; aggregates QC `skill_vs_no_skill_evaluation` thresholds |
| `src/intelligence/task-prompt-compiler/index.ts` | ADDED | module public surface |
| `src/intelligence/skills/definitions/taskPromptCompilerS13G.ts` | ADDED | typed `SkillDefinition` (23 MUST rules `TC-R1..23` / 19 procedure `TC-P1..19` / 12 verification `TC-V1..12`), synthesis-only, `exclusions` scope out future-stage work |
| `src/intelligence/skills/index.ts` | MODIFIED | registers `taskPromptCompilerS13G` as the 10th reference Skill (mechanical, identical to every prior S13x) |
| `tests/task-prompt-compiler/fixtures.ts` | ADDED | minimal valid upstream artifacts, caller host defs, 4 positive + 10 negative inputs, `goodPackage`, `DeterministicTaskCompilationModelProvider`; does **not** import `fixtureTruth.ts` |
| `tests/task-prompt-compiler/fixtureTruth.ts` | ADDED | hand-authored frozen ground truth for the 4 positive fixtures (sha256 `11761ba2a90bdd86992f008cde0ef1c16bcfa3ba13bb18c884cfa97119b7915a`); only the comparator reads it |
| `tests/task-prompt-compiler/taskCompilation.test.ts` | ADDED | `SKILL-ARTIFACT-1..4` + T1–T50 + 4 anchors (81 `it()` cases) |
| `tests/implementation-planning/implementationPlanning.test.ts` | MODIFIED | one mechanical prior-test count relaxation: exact 9-id `toEqual([...])` → `slice(0,9)` equality + `length ≥ 9`. Mirrors S13D→E and S13E→F. **Not** an S13F semantic change. |
| `brain-bootstrap/reports/S13G-task-prompt-compiler-verification.md` | ADDED | full verification report incl. the T1→T50 mapping (§8) and measured figures (§6) |
| `brain-bootstrap/STATE.yaml` | MODIFIED | `current_step: S13G`, `S13G: PASS`, `repository.task_prompt_compiler` added (closed-PASS shape); `repository:` `commits`/`head_sha` refreshed in the `docs:` follow-up commit |
| `brain/context/CURRENT.md` | MODIFIED | points to this handoff; HEAD sha placeholder set by the `docs:` follow-up commit |
| `brain/context/handoffs/2026-08-27T104500Z-s13g-to-s13h-handoff.md` | ADDED | this handoff |

No S13G Part A canonical artifact, and no S11/S13A–S13F canonical artifact (markdown/YAML spec or
TypeScript implementation), was semantically touched by this closure. `src/core/` is untouched. No
`package.json` / `package-lock.json` change. `S13G_CHATGPT_PART_A_CANONICAL.md` remains untracked and
retained.

# 8. next exact action

**Exactly one action:** run a fresh-session **read-only** independent verification of S13G, analogous
to `S13F_FRESH_INDEPENDENT_VERIFICATION.md`. It must:

1. confirm branch `main`, record HEAD, confirm `== origin/main`;
2. confirm `git diff f7ef33537ef5d61402f6d6b8a783db6da25db7b2 -- brain-bootstrap/skills/TASK_PROMPT_COMPILER_SKILL_S13G.md brain-bootstrap/quality-contracts/S13G_TASK_PROMPT_COMPILER_DEEP.yaml brain-bootstrap/specs/EXECUTION_PACKAGE_CONTRACT_S13G.md` is **empty**;
3. confirm `sha256sum S13G_CHATGPT_PART_A_CANONICAL.md` == `fc5ef8e83e6669d06727785a4c17442543a1403ca3c1a4d81ce1bbd7b3a3417f`;
4. with Node `v24.19.0`: `npm run typecheck` (0 errors), `npm test` (`533/533`), `rm -rf dist && npm run build`, post-build `npm test` (`533/533`);
5. independently re-measure the Skill-vs-no-Skill figures from `dist/` and check they match report §6 (baseline `56`, skill `108`, `+52`, hard `48/48`, `0` boundary violations, 9 improved dimensions);
6. return:

```text
VERIFICATION RESULT
Step: S13G
Status: PASS
```

**S13H remains `NOT_STARTED`** until that verification returns `Status: PASS`. Do not start, inspect,
or author S13H before then. When S13H is later authorized, confirm its exact canonical objective and
Entrada/Salida directly from `.claude/skills/brain-build-day-bootstrap/SKILL.md` (not from memory);
S13H is inside the ChatGPT Authoring Gate (`3.1`, "todos los S13x").

# 9. do-not-do

- Do not start S13H (or any later step) — not even inspection or authoring — until the fresh independent verification of S13G returns `Status: PASS`.
- Do not silently alter D-056 through D-060 or any prior closed decision — they are closed decisions, not open questions.
- Do not create a task-prompt-compiler `AgentDefinition`, register one in `src/intelligence/agent-definitions/`, or add `role`/id branching for compilation in `src/core/`. S13G is `SKILL_ONLY` by Part A.
- Do not modify `brain-bootstrap/skills/TASK_PROMPT_COMPILER_SKILL_S13G.md`, `brain-bootstrap/quality-contracts/S13G_TASK_PROMPT_COMPILER_DEEP.yaml`, or `brain-bootstrap/specs/EXECUTION_PACKAGE_CONTRACT_S13G.md` without returning to the ChatGPT Authoring Gate first if a semantic contradiction is found. At closure `git diff f7ef335 -- <those 3 files>` must stay empty.
- Do not include `S13G_CHATGPT_PART_A_CANONICAL.md` in any commit; retain it until the fresh independent verification is done.
- Do not build a Workflow Runtime, Task Executor, execution graph, Context Pack compiler, Capability Registry, MCP layer, provider/tool binding, BUILD stage, S13H repository-git-workflow, Agent Factory, or Skill Factory.
- Do not treat the Skill-vs-no-Skill comparison as emergent measurement — it is a content-derived maximal contrast (OI-S13G-03).
- Do not build a fake "perfect arbitrary-string secret detector" — secret handling tests use explicit tagged/known secret fields only.
- Do not trust this Handoff or `CURRENT.md` blindly — independently re-verify branch/HEAD/sync/`STATE.yaml` and runtime before continuing.

# 10. assumptions needing revalidation

| Assumption ID | Assumption | Impact if wrong | Revalidate before |
|---|---|---|---|
| A-019 | `origin/main` has not advanced past this closure's commits since they were pushed | Stale HEAD claim; a later session would build on an outdated base | Before any S13H work — re-run `git fetch origin` + `git rev-parse origin/main` |
| A-020 | Node 24 (`v24.19.0` via nvm) remains required for all QA and may be shadowed by a separate Node on default `PATH` | `npm`/`tsc`/`vitest` could silently run under the wrong Node version | Every new shell — re-run `node --version` after prepending `/home/yosman/.nvm/versions/node/v24.19.0/bin` |
| A-021 | The fresh independent verification of S13G has **not** yet been run | S13H could be started prematurely against an unverified S13G | Before S13H — confirm a `VERIFICATION RESULT / Step: S13G / Status: PASS` exists |

# 11. Relevant Context References

**Current Spec:** `brain-bootstrap/specs/EXECUTION_PACKAGE_CONTRACT_S13G.md` (S13G — canonical, now fully implemented); semantic anchor `brain-bootstrap/specs/SPEC_CONTRACT.md` Stage 11 `TASK COMPILATION` (lines 595–641)

**Verification Report:** `brain-bootstrap/reports/S13G-task-prompt-compiler-verification.md`

**Authoring preflight / evidence pack:** `S13G_AUTHORING_PREFLIGHT.md` (forbidden-key collision, S05-vs-Stage-11 context tension, execution-model signals)

**Part A transfer file (retained, untracked):** `S13G_CHATGPT_PART_A_CANONICAL.md` (sha256 `fc5ef8e83e6669d06727785a4c17442543a1403ca3c1a4d81ce1bbd7b3a3417f`)

**Precedent reports:** `brain-bootstrap/reports/S13F-implementation-planning-verification.md`, `…/S13E-agent-engineering-verification.md`, `…/S13D-software-architecture-verification.md`, `…/S13C-deep-research-verification.md`, `…/S13B-knowledge-gap-analysis-verification.md`, `…/S13A-requirements-discovery-verification.md`

**Precedent fresh verification:** `S13F_FRESH_INDEPENDENT_VERIFICATION_RESULT.md`

# 12. Staleness / Revalidation Triggers

Revalidate this Handoff if: repository HEAD differs from the commit this handoff was created alongside;
branch differs from `main`; worktree changes unexpectedly; Node version resolves to something other
than `v24.19.0`; `brain-bootstrap/STATE.yaml` shows a different status for S13G than `PASS` or a
different status for S13H than `NOT_STARTED`; any S13G Part A artifact's bytes differ from `f7ef335`;
the transfer sha256 differs; a required test/check now fails (baseline was `typecheck: 0 errors`,
`tests: 533/533`).

# 13. Close Verification

- [x] objective is current and bounded.
- [x] branch/HEAD/status independently observed.
- [x] verified completed work contains only verified items.
- [x] commands/evidence are reproducible or inspectable.
- [x] decisions have authority/status.
- [x] open issues are explicit.
- [x] changed files are listed.
- [x] next exact action is exactly one action.
- [x] do-not-do is explicit.
- [x] assumptions needing revalidation are visible.
- [x] no secrets are included.
- [x] full transcript is not copied into this Handoff.

**Handoff readiness:**
`READY`
