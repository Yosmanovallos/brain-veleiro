export const positiveFixtureIds = Array.from({length:12},(_,i)=>`FX-POS-${String(i+1).padStart(3,"0")}`);
export const negativeFixtureIds = Array.from({length:26},(_,i)=>`FX-NEG-${String(i+1).padStart(3,"0")}`);
export const hardInvariantIds = Array.from({length:32},(_,i)=>`S14H-HI-${String(i+1).padStart(3,"0")}`);
export const unsafeCounterIds = Array.from({length:10},(_,i)=>`UC${String(i+1).padStart(2,"0")}`);
